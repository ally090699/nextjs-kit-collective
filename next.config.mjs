/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
      basePath: '/nextjs-kit-collective',
      images: {
        unoptimized: true,
      },
};

export default nextConfig;
